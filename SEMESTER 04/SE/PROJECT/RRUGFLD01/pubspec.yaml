name: rrugfld01
description: "RASHTRIYA RAKSHA UNIVERSITY GANDHINAGAR FACILITY LOCATOR DRAFT 01"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.1.1    # For OpenStreetMap integration
  latlong2: ^0.9.1        # To handle Lat/Lon points
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rru_college_map.osm  # Add the OSM file as an asset
