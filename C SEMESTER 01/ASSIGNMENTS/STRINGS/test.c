#include <stdio.h>
int main ()
{
    // typedef char * string;
    // string a;
    // scanf("%[^\n]s", a);
    // printf("%s", a);
    // a[3]='b';
    // puts(a)
    char del;
    printf("Enter the character to be removed: ");
    scanf("%c", del);

}
